import React from 'react';
export type AclMatrixData={roles:{id:number;name:string;slug:string}[];permissions:{id:number;name:string;slug:string;group?:string|null}[];matrix:Record<string,string[]>};
export function AclMatrix({data,onChange}:{data:AclMatrixData;onChange?:(role:string,permission:string,allowed:boolean)=>void}){return <div className="overflow-auto"><table className="min-w-full text-sm"><thead><tr><th className="p-2 text-left">Permission</th>{data.roles.map(r=><th key={r.slug} className="p-2">{r.name}</th>)}</tr></thead><tbody>{data.permissions.map(p=><tr key={p.slug} className="border-t"><td className="p-2">{p.slug}</td>{data.roles.map(r=>{const checked=data.matrix[r.slug]?.includes(p.slug)??false;return <td key={r.slug} className="p-2 text-center"><input type="checkbox" checked={checked} onChange={e=>onChange?.(r.slug,p.slug,e.target.checked)}/></td>})}</tr>)}</tbody></table></div>}
